import { combineReducers } from 'redux';

export default combineReducers({
    shows: (state = {}, action) => {
        switch (action) {
            default: 
                return state;
        }
    }
});